이전 출력은 JSON 스키마를 위반했습니다. 아래 조건으로만 ‘수정된 JSON’만 출력하세요:
- 누락 필드는 채움. prob<0 또는 NaN은 0으로 보정.
- 모든 prob 합이 0이면, 후보를 균등 분배.
- prob 합이 1±0.05가 되도록 3자리 반올림 후 정규화.
- 마크다운/설명 없이 STRICT JSON만 출력.

입력(JSON): {BROKEN_JSON}
스키마: VS 템플릿에서 요구한 동일 스키마

